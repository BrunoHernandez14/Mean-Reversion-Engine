# Mean-Reversion-Engine
This project uses Hurst Exponent, OU Process, and Z-Score to compute Mean Reversion speed, regime, and deviation. It also uses two databases and real time websocket data from Kraken to compute mid price candles for one database and the second for calculated values from those candles.
